function VerificationContainer({children}) {
    return (
        <div className="w-full h-screen flex items-center justify-center bg-gray-950">
            {children}
        </div>
    );
}

export default VerificationContainer;